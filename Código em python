from abc import ABC, abstractmethod

class Poligono(ABC):
    @abstractmethod
    def area(self):
        pass

if __name__ == "__main__":
    p = [None] * 10  # Cria uma lista com 10 elementos, todos inicialmente None
